import 'package:flutter/material.dart';

class AccountDetails extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      // TODO: embed webview snooze.com/account
      child: new Text('TODO: webview with accountsettings'),
    );
  }
}