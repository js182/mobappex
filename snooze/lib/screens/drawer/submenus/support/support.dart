import 'package:flutter/material.dart';

class Support extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      //TODO: embed webview snooze.com/support
      child: new Text('TODO: webview with FAQ, Capsule problems, App problems, report malfunction, contact us..'),
    );
  }
}
